class TeamsStanding {
	constructor({ pos, name, id, points, url, nationality }) {
		this.pos = pos;
		this.name = name;
		this.id = id;
		this.points = points;
		this.url = url;
		this.nationality = nationality;
	}
}

export default TeamsStanding;
